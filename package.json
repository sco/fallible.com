{
  "name": "fallible.com",
  "version": "1.0.0",
  "scripts": {
    "dev": "npx @11ty/eleventy --serve",
    "build": "npx @11ty/eleventy && npx pagefind --site _site"
  },
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "@types/node": "^26.1.1",
    "pagefind": "^1.5.2",
    "wrangler": "^4.107.0"
  }
}

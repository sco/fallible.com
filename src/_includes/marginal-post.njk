---
layout: marginal-base.njk
---

<article>
  <h2>{{ title | typTitle }}</h2>
  <p class="mc-post-meta">{{ date | postDate }}</p>
  {{ content | safe }}
</article>

{% set older = collections.marginalPosts | getNextCollectionItem %}
{% set newer = collections.marginalPosts | getPreviousCollectionItem %}
<nav class="mc-nav">
  <span>{% if older %}&larr; <a href="{{ older.url }}">{{ older.data.title | typTitle }}</a>{% endif %}</span>
  <a href="/marginal/">All Posts</a>
  <span>{% if newer %}<a href="{{ newer.url }}">{{ newer.data.title | typTitle }}</a> &rarr;{% endif %}</span>
</nav>

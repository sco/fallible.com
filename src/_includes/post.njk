---
layout: base.njk
---

<article>
  <h2 class="title">{{ title | typTitle }}</h2>
  <p style="margin-top: 0; margin-bottom: 1.5em;"><em>{{date | postDate}}</em></p>
  {{ content | safe }}
</article>

{% if comments and comments.length > 0 %}
<section class="comments" style="margin-top: 4em">
  <h3>Comments</h3>
  {% for comment in comments %}
  <div class="comment" style="margin-bottom: 2em">
    <p class="comment-text">{{ comment.text | safe }}</p>
    <p><em>&mdash;{{ comment.author}}, {{ comment.date | postDate }}</em></p>
  </div>
  {% endfor %}
</section>
{% endif %}

{% set older = collections.mainPosts | olderPost(page.url) %}
<nav style="margin-top: 4em; font-size: 1.2em; display: flex; justify-content: space-between; align-items: baseline;">
  <span>{% if older %}Previous: <a href="{{ older.url }}">{{ older.data.title | typTitle }}</a>{% endif %}</span>
  <a href="/archive">All Posts</a>
</nav>
